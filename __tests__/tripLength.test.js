import { tripLength } from "../src/client/js/tripLength"

describe('Tests function viability', () => {
    test('Test tripLength function is defined', () => {
        expect(tripLength).toBeDefined()
    })
})